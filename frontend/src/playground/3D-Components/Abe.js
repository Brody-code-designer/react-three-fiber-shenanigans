/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { useEffect, useRef } from 'react'
import { useGLTF, useAnimations } from '@react-three/drei'
import abe from "../../3D-assests/abe.glb"

export default function Abe({ ...props }) {
  const group = useRef()
  const { nodes, materials, animations } = useGLTF(abe)
  const { actions } = useAnimations(animations, group)

console.log('actions', actions);

useEffect( () => {
  actions.flipKick.reset().fadeIn(0.5).play()
  return () => actions.flipKick.fadeOut(0.5)
}, [actions])

  return (
    <group ref={group} {...props} dispose={null}>
      <group rotation={[Math.PI / 2, 0, 0]} scale={0.01}>
        <primitive object={nodes.mixamorigHips} />
        <skinnedMesh
          geometry={nodes.Ch39.geometry}
          material={materials['Ch39_Body.001']}
          skeleton={nodes.Ch39.skeleton}
        />
      </group>
    </group>
  )
}

useGLTF.preload(abe)
